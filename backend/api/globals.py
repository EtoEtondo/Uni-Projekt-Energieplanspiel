# globale variablen, welche für die Berechnung (main.py) verwendet werden zu bestimmung des fortschritts
# Sind nur! im RAM d.h. bei neustart von Django sind diese nicht mehr vorhanden

# für die laufenden prozesse der institution -> instructor -> schedule
proc = {}
# für den output des laufenden prozesses der institution -> instructor -> schedule
output = {}
