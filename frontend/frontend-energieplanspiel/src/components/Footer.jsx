import {FooterWrapper} from '../styling/styled-components/Wrapper';
import Button from '@mui/material/Button';
import AddIcon from '@mui/icons-material/Add';

export const Footer = ({}) => {
    return <FooterWrapper />
  };